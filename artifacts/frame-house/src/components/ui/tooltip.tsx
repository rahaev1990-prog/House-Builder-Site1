import * as React from "react";

// Placeholder for TooltipProvider to satisfy App.tsx imports without adding complex radix dependencies
export function TooltipProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
